# Template for GitHub Actions with managed identity
# These should be set as GitHub repository secrets

# Azure Authentication (Managed Identity/OIDC)
AZURE_CLIENT_ID=your-app-registration-client-id
AZURE_TENANT_ID=your-azure-tenant-id
AZURE_SUBSCRIPTION_ID=your-azure-subscription-id

# Storage Configuration for CI/CD
STORAGE_AZURE_ACCOUNT_NAME=your-storage-account-name
STORAGE_AZURE_USE_MANAGED_IDENTITY=true
STORAGE_AZURE_CONTAINER_NAME=your-ci-container

# Fallback for transition period (can be removed once fully migrated)
STORAGE_AZURE_CONNECTION_STRING=your-connection-string-fallback
