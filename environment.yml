name: cb-trade
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - pip     # Ensure pip is installed by Conda
  - pandas
  - numpy
  - pydantic
  - pydantic-settings
  - azure-storage-blob
  - fastapi
  - uvicorn
  - websockets
  - pytest
  - pytest-asyncio
  - pytest-mock
  - pytest-dotenv
  - pytest-timeout
  - aiofiles
  - python-dotenv
  - pip:
    - ccxt  # Install ccxt using pip
    - flake8 # Added linter
    - black  # Added formatter
